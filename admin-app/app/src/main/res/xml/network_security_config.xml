<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- الثقة بالسيرفر الأساسي -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">inspiredownloader.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- السماح بـ HTTP للتطوير (للسيرفرات المحلية) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>

    <!-- في وضع Debug: السماح بكل الـ HTTP (للتطوير) -->
    <!-- يُفعَّل تلقائياً عند android:debuggable="true" -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
